<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.CSVAnalyzer"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.components.AdvancedCharts"
            android:exported="false"
            android:label="@string/title_activity_advanced_charts"
            android:theme="@style/Theme.CSVAnalyzer" />
        <activity
            android:name=".ui.screens.ui.PdfReportScreen"
            android:exported="false"
            android:label="@string/title_activity_pdf_report_screen"
            android:theme="@style/Theme.CSVAnalyzer" />
        <activity
            android:name=".ui.screens.PdfReportScreen"
            android:exported="false"
            android:label="@string/title_activity_pdf_report_screen"
            android:theme="@style/Theme.CSVAnalyzer" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CSVAnalyzer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>